import GalleryComponent from "@/components/gallery-component"
export const metadata = {
    "title": "Galley | Chillfeast",
    "desciption": "Look at the works we done for ou clients"
}
export default function Gallery()
{
    return(
       <GalleryComponent />
    )
}